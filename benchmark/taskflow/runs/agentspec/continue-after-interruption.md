Continue the native AgentSpec TaskFlow workflow from the existing repository
state. Brainstorm and Define were already written and committed on
`feature/taskflow-task-manager`; do not regenerate or discard them.

Treat the product decisions in those artifacts as approved because they derive
from the frozen common brief. Complete the remaining native phases: Design,
Build, tests, code review and fixes, Ship/closure evidence, coherent commits,
and root-level `PR.md`. Do not publish a remote or pull request. Run every
documented test, lint, type check, and build command. Finish with a clean
working tree.

Do not ask product questions whose answers already exist in the committed
Brainstorm/Define artifacts. If a framework-required approval gate appears,
approve only when the artifact is consistent with the frozen brief; otherwise
correct it first.
