[project]
name = "taskflow-benchmark"
version = "1.0.0"
requires-python = ">=3.11"
dependencies = [
  "httpx==0.28.1",
  "jsonschema==4.25.1",
]

[dependency-groups]
dev = [
  "pytest==8.4.2",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
