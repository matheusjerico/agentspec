---
name: agentspec-generate-visual-plan
description: |
  Generate a visual HTML implementation plan — detailed feature specification with state machines, code snippets, and edge cases
---

# Generated AgentSpec adapter

Do not edit this file. The source of truth is `.claude/commands/visual-explainer/generate-visual-plan.md`.

Before acting, open `.claude/commands/visual-explainer/generate-visual-plan.md`; read it completely and follow it as the
authoritative workflow. Use Codex-native tools for equivalent Claude-specific
tool names. Preserve workspace paths under `.claude/`. If a required capability
has no Codex equivalent, identify that capability and stop at that boundary
instead of silently skipping it.
