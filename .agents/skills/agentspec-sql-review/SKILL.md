---
name: agentspec-sql-review
description: |
  SQL-specific code review — delegates to sql-optimizer + code-reviewer agents
---

# Generated AgentSpec adapter

Do not edit this file. The source of truth is `.claude/commands/data-engineering/sql-review.md`.

Before acting, open `.claude/commands/data-engineering/sql-review.md`; read it completely and follow it as the
authoritative workflow. Use Codex-native tools for equivalent Claude-specific
tool names. Preserve workspace paths under `.claude/`. If a required capability
has no Codex equivalent, identify that capability and stop at that boundary
instead of silently skipping it.
