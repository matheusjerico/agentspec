---
name: agentspec-sync-context
description: |
  Sync project context to CLAUDE.md by analyzing codebase patterns and conventions
---

# Generated AgentSpec adapter

Do not edit this file. The source of truth is `.claude/commands/core/sync-context.md`.

Before acting, open `.claude/commands/core/sync-context.md`; read it completely and follow it as the
authoritative workflow. Use Codex-native tools for equivalent Claude-specific
tool names. Preserve workspace paths under `.claude/`. If a required capability
has no Codex equivalent, identify that capability and stop at that boundary
instead of silently skipping it.
