---
name: agentspec-create-kb
description: |
  Create a KB domain — light single-pass by default, --validated for a source-verified high-assurance build, --audit for KB health
---

# Generated AgentSpec adapter

Do not edit this file. The source of truth is `.claude/commands/knowledge/create-kb.md`.

Before acting, open `.claude/commands/knowledge/create-kb.md`; read it completely and follow it as the
authoritative workflow. Use Codex-native tools for equivalent Claude-specific
tool names. Preserve workspace paths under `.claude/`. If a required capability
has no Codex equivalent, identify that capability and stop at that boundary
instead of silently skipping it.
