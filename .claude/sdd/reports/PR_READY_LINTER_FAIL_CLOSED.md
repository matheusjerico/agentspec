# PR READY: Linter Fail Closed

> Readiness handoff generated by Ship after Gate S (`WORKFLOW_CONTRACTS.yaml`
> → `pr_readiness`). `/create-pr` consumes this artifact: it revalidates every
> `mutable: yes` row immediately before publication and pastes the description
> below — never reconstructing it. Delete this file once the PR URL exists.

## Metadata

| Attribute | Value |
|-----------|-------|
| **Feature** | LINTER_FAIL_CLOSED |
| **Generated** | 2026-07-30 03:45Z |
| **Branch** | feat/auto-linter-fail-closed |
| **Ship HEAD SHA** | bd58c54 (build HEAD at ship time; the ship commit on top is docs-only — archive move + this artifact, no code) |
| **Status** | ✅ Ready |

---

## Readiness Checklist

| Dimension | Item | Status | Evidence | Mutable? |
|-----------|------|--------|----------|----------|
| branch | working_tree_clean | ✅ | `git status --short` clean after the ship commit | yes — revalidate |
| branch | base_resolved | ✅ | merge-base with origin/main (6164c32) + conflict-free merge-tree dry-run | yes — revalidate |
| quality | lint | ✅ | BUILD_REPORT → Lint (shellcheck exit 0; ruff clean on touched files, reviewer-run) | no |
| quality | types | ⏭️ | BUILD_REPORT → Type Check (no mypy gate; 45 unit tests cover the logic) | no |
| quality | tests | ✅ | BUILD_REPORT → Tests (410/410: 172 root + 238 spec-linter) | yes — re-run |
| quality | build | ✅ | ./build-plugin.sh exit 0 incl. parity 21/21 | yes — re-run |
| traceability | must_requirements_covered | ✅ | filled Traceability Matrix (6/6 MUSTs + SHOULD + COULD, all Pass) | no |
| review | branch_verdict | ✅ | Review Verdict: clean (closing verification) | no |
| review | blocking_findings_open | ✅ | findings table: 0 OPEN Critical/Important (2 minors fixed in round 1) | no |
| review | verdict_unchanged | ✅ | HEAD = ship commit, docs-only on top of reviewed bd58c54 (no code drift) | yes — revalidate |
| delivery | migration_plan | ✅ | n/a recorded — no vocabulary change; absent blocks byte-compatible; only present-invalid input and malformed rows change outcome (DESIGN Schema Evolution Plan: none required) | no |
| delivery | rollback_plan | ✅ | revert the PR — prior tolerance restored; no data or state involved | no |
| delivery | residual_risks | ✅ | define-side risk_profiles wiring remains isinstance-silent (documented out of scope — conduct data, no linter rules) | no |

---

## PR Description (generated — paste, don't rebuild)

### Problem & solution

A Codex cross-model review of PRs #5–#13 (docs/reviews/2026-07-29-codex-review-prs-5-13.md, included in this PR) found three fail-open paths in the spec-linter, all one class: silent drop or silent fallback where fail-closed is required. A malformed opt-in config block silently disarmed its rule family; a truncated Traceability Matrix row vanished from MUST coverage; a truncated Task Reviews row could hide a dirty verdict at low risk. This PR closes all three: present-but-invalid configuration is an exit-2 operational error on both phases (absence stays the dormant path, byte-compatible), and malformed rows are FAIL findings naming the offending row.

### Scope & out of scope

**In scope:** cli.py fail-closed wiring (4 build-side + 2 design-side blocks + key-presence routing), build-side traceability arming requiring `verification_types`, three malformed-row FAIL rules (`BR.matrix_row_malformed`, `TX.matrix_row_malformed`, `BR.task_review_row_malformed`), v3.17.0 + history, USAGE.md fail-closed note, 46 tests.
**Out of scope:** new configuration vocabulary; retro-linting archives; template files (authoring surfaces); `pr_readiness`/`commit_parallel`/`risk_profiles` blocks (conduct data, no linter wiring).

### Requirements delivered

- REQ-001 (MUST): build-side fail-closed wiring — present-but-non-mapping → exit 2 naming block + type; traceability validates verification_types before arming
- REQ-002 (MUST): design-side wiring + key-presence routing — invalid values can never silently downgrade to the plain contract
- REQ-003/004/005 (MUST): the three malformed-row FAIL rules, severity independent of risk gates
- REQ-006 (MUST): v3.17.0 + history; root history pin updated
- REQ-007 (SHOULD): TDD RED-first tests (41 verified failing pre-implementation)
- REQ-008 (COULD): USAGE.md fail-closed note

### Test strategy

TDD RED-first: 45 new unit tests written and verified failing before implementation — parametrized config-drift grid (6 blocks × null/string/list/int → exit 2; absent → dormant green), malformed-row cardinality grids (2–7 cells build, 2–5 design), the hidden-dirty-verdict repro at low risk, placeholder rows, and intact-row controls. Full suites: 172 root + 238 spec-linter; plugin parity 21/21; dogfood — the real WORKFLOW_CONTRACTS.yaml arms cleanly under the hardened wiring, and this increment's own BUILD_REPORT passed the hardened gate with zero findings.

### Traceability Matrix (summarized)

| REQ | Priority | Tasks | Tests | Result |
|-----|----------|-------|-------|--------|
| REQ-001 | MUST | TASK-CLI-001 | tools/spec-linter/tests/test_cli.py | Pass |
| REQ-002 | MUST | TASK-CLI-001 | tools/spec-linter/tests/test_cli.py | Pass |
| REQ-003 | MUST | TASK-LINT-001 | tests/test_build_report_contract.py | Pass |
| REQ-004 | MUST | TASK-LINT-002 | tests/test_design_phase_contract.py | Pass |
| REQ-005 | MUST | TASK-LINT-001 | tests/test_build_report_contract.py | Pass |
| REQ-006 | MUST | TASK-DATA-001, TASK-TEST-001 | tests/test_workflow_metrics.py | Pass |
| REQ-007 | SHOULD | 3 code tasks | all three linter test files | Pass |
| REQ-008 | COULD | TASK-DOC-001 | grep verification | Pass |

### Residual risks

The define-side `risk_profiles` block remains isinstance-silent — documented out of scope (conduct data with no linter rules to disarm); revisit if it ever gains enforcement. No other residuals.

### Migration & rollback

Migration: none — no vocabulary change; absent blocks keep byte-compatible dormant behavior; well-formed repos see zero difference. Only present-invalid config (was: silent disarm → now: exit 2) and malformed rows (was: silent drop → now: FAIL) change outcome, which is the point. Rollback: revert the PR; prior tolerance restored.

### Review findings

Verdict: **clean** (1/2 fix rounds). Two minor warnings — a stale "disclosed residual" comment surviving on the design-side parser, and mid-file `import pytest as _pytest` (ruff E402) in two test files — both fixed and re-verified (ruff clean, parity byte-identical). The reviewer independently reproduced the Codex `traceability: []` scenario against main (silently tolerated) vs this branch (exit 2), and the truncated-MUST-row scenario likewise.

### Validation instructions

```bash
rtk proxy python3 -m pytest tests/ -q                       # 172 passed
cd tools/spec-linter && rtk proxy python3 -m pytest -q      # 238 passed
./build-plugin.sh                                           # exit 0, parity green
# Live repro of Codex finding 1 (config drift):
python3 - <<'EOF'
import yaml, subprocess, tempfile, pathlib
data = yaml.safe_load(open('.claude/sdd/architecture/WORKFLOW_CONTRACTS.yaml'))
data['traceability'] = []
with tempfile.TemporaryDirectory() as d:
    f = pathlib.Path(d)/'contracts.yaml'; f.write_text(yaml.safe_dump(data))
    r = subprocess.run(['python3','-m','spec_linter.cli',
        '../../.claude/sdd/archive/LINTER_FAIL_CLOSED/BUILD_REPORT_LINTER_FAIL_CLOSED.md',
        '--phase','build','--contracts-file',str(f)], cwd='tools/spec-linter', capture_output=True, text=True)
    print(r.returncode, r.stderr.strip())   # expect: 2, ERROR naming traceability
EOF
```

### Screenshots (when applicable)

n/a — no UI.

---

## Gaps (when any item failed — otherwise "None")

None
