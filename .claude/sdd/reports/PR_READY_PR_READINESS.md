# PR READY: Pr Readiness

> Readiness handoff generated by Ship after Gate S (`WORKFLOW_CONTRACTS.yaml`
> → `pr_readiness`). `/create-pr` consumes this artifact: it revalidates every
> `mutable: yes` row immediately before publication and pastes the description
> below — never reconstructing it. Delete this file once the PR URL exists.

## Metadata

| Attribute | Value |
|-----------|-------|
| **Feature** | PR_READINESS |
| **Generated** | 2026-07-29 21:20Z |
| **Branch** | feat/auto-pr-readiness |
| **Ship HEAD SHA** | 870a904 (build HEAD at ship time; the ship commit on top is docs-only — archive move + this artifact, no code) |
| **Status** | ✅ Ready |

---

## Readiness Checklist

| Dimension | Item | Status | Evidence | Mutable? |
|-----------|------|--------|----------|----------|
| branch | working_tree_clean | ✅ | `git status --short` clean after the ship commit | yes — revalidate |
| branch | base_resolved | ✅ | merge-base 58c0e18 with origin/main + conflict-free merge-tree dry-run | yes — revalidate |
| quality | lint | ✅ | BUILD_REPORT → Verification Results → Lint (shellcheck clean, exit 0) | no |
| quality | types | ⏭️ | BUILD_REPORT → Type Check (N/A — no code changes) | no |
| quality | tests | ✅ | BUILD_REPORT → Tests (334/334: 162 root + 172 spec-linter) | yes — re-run |
| quality | build | ✅ | ./build-plugin.sh exit 0 incl. Step 5e parity | yes — re-run |
| traceability | must_requirements_covered | ✅ | filled Traceability Matrix (6/6 MUSTs + 1 SHOULD, all Pass) | no |
| review | branch_verdict | ✅ | Review Verdict: clean-with-minors | no |
| review | blocking_findings_open | ✅ | findings table: 0 OPEN Critical/Important (F1/F2 closed in round 1) | no |
| review | verdict_unchanged | ✅ | HEAD = ship commit, docs-only on top of reviewed 870a904 (no code drift) | yes — revalidate |
| delivery | migration_plan | ✅ | n/a recorded — additive contract block + new template; adopters opt in by shipping with the new sdd-ship (DESIGN Schema Evolution Plan: none required) | no |
| delivery | rollback_plan | ✅ | revert the PR — contract block, template, and skill sections are self-contained; no data or state migration | no |
| delivery | residual_risks | ✅ | 1 recorded minor: legacy Quality Checklist in create-pr.md names skipped-template fields (cosmetic, legacy path only) | no |

---

## PR Description (generated — paste, don't rebuild)

### Problem & solution

PR readiness lived as tribal knowledge scattered across Build, Ship, and `/create-pr` — each phase re-derived "is this branch ready?" its own way, and nothing prevented publishing stale readiness (e.g., commits after the review). This PR makes readiness a shared contract: a `pr_readiness` block (WORKFLOW_CONTRACTS.yaml v3.15.0) with 13 items across branch/quality/traceability/review/delivery dimensions, each mapped to evidence the pipeline already produces. Ship validates the checklist after Gate S and generates a PR_READY artifact; `/create-pr` consumes it, revalidating the mutable subset immediately before publication and refusing on drift.

### Scope & out of scope

**In scope:** the contract block (data), PR_READY_TEMPLATE.md (checklist + description skeleton + Gaps), sdd-ship generation step (non-blocking), create-pr consumption with skip-gate over legacy Steps 1–5, PR boundaries in sdd-build/sdd-autopilot, 12 documental tests.
**Out of scope:** new linter rules (every item's evidence is already linter-enforced by Increments 1–7 — AT-009 verifies a zero diff under `tools/`), auto-publication (external publication always requires explicit user intent), CI integration.

### Requirements delivered

- REQ-001 (MUST): `pr_readiness` contract block — 5 dimensions, 13 items, evidence + mutable mapping, per-consumer behavior
- REQ-002 (MUST): PR_READY template with readiness checklist and §14.3 description skeleton
- REQ-003 (MUST): Ship generates the artifact after Gate S, non-blocking (failures fill Gaps, never a new ship blocker)
- REQ-004 (MUST): `/create-pr` consumes the artifact — mutable revalidation before publication, refuse-on-drift, paste-never-rebuild
- REQ-005 (MUST): Build produces evidence, never opens PRs; autopilot Stage 7 consumes, Stage 8 close-run commit deletes
- REQ-006 (MUST): explicit-user-intent guard on publication
- REQ-007 (SHOULD): documental test coverage for all conduct anchors

### Test strategy

Documental anchors (the increment is conduct + data + template — no runtime code): 12 new tests in `tests/test_pr_readiness.py` pin the contract block shape, the 13 checklist rows and Ship HEAD SHA anchor in the template, the ship generation step, the create-pr skip-gate and mutable-revalidation conduct, the build/autopilot boundaries, and the intent guard. Full suites: 162 root + 172 spec-linter (untouched), plugin build with Step 5e parity green.

### Traceability Matrix (summarized)

| REQ | Priority | Tasks | Tests | Result |
|-----|----------|-------|-------|--------|
| REQ-001 | MUST | TASK-CONTRACT-001 | tests/test_pr_readiness.py | Pass |
| REQ-002 | MUST | TASK-TMPL-001 | tests/test_pr_readiness.py | Pass |
| REQ-003 | MUST | TASK-SKILL-001 | tests/test_pr_readiness.py | Pass |
| REQ-004 | MUST | TASK-CMD-001 | tests/test_pr_readiness.py | Pass |
| REQ-005 | MUST | TASK-SKILL-002 | tests/test_pr_readiness.py | Pass |
| REQ-006 | MUST | TASK-CONTRACT-001 | tests/test_pr_readiness.py | Pass |
| REQ-007 | SHOULD | TASK-TEST-001 | tests/test_pr_readiness.py | Pass |

### Residual risks

One recorded minor: create-pr.md's legacy Quality Checklist still names fields from the skipped legacy template — cosmetic, affects only the no-artifact path, reviewer-classified non-blocking. Accepted trade-off: no `--phase pr` linter pack yet (evidence already enforced upstream; recorded as future work).

### Migration & rollback

Migration: n/a — additive contract block and a new template; existing flows without a PR_READY artifact run the legacy create-pr conduct unchanged (AT-008, byte-identical legacy body). Rollback: revert the PR; no data or state involved.

### Review findings

Verdict: **clean-with-minors** (1/2 fix rounds). F1 (skip-gate missing) and F2 (phantom evidence anchor) — Important, fixed with regression tests; F3–F7 Minor, fixed (Ship HEAD SHA anchor, merge-tree probe, close-run commit naming, second template test, {FEATURE} resolution rule); one cosmetic legacy residual recorded.

### Validation instructions

```bash
rtk proxy python3 -m pytest tests/ -q                       # 162 passed
cd tools/spec-linter && rtk proxy python3 -m pytest -q      # 172 passed
./build-plugin.sh                                           # exit 0, Step 5e parity
git diff main -- tools/                                     # empty (zero linter changes)
```

### Screenshots (when applicable)

n/a — no UI.

---

## Gaps (when any item failed — otherwise "None")

None
